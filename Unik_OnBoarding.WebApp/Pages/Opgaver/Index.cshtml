@page
@model Unik_OnBoarding.WebApp.Pages.Opgaver.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OpgaverEntity[0].OpgaveName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OpgaverEntity[0].Beskrivelse)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OpgaverEntity[0].RowVersion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.OpgaverEntity) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OpgaveName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Beskrivelse)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RowVersion)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.OpgaveId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.OpgaveId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.OpgaveId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@page
@model IndexModel

@{
	ViewData["Title"] = "Index";
}

<div class="container p-3">
	<div class="row pt-4">
		<div class="col-6">
			<h3 class="text-primary">Kompetencer List</h3>
		</div>
		<div class="col-6 text-end">
			<a asp-page="Create" class="btn btn-warning">
				<i class="bi bi-plus-square"></i> &nbsp;Create Kompetencer
			</a>
		</div>
	</div>

	<br/><br/>

	<table class="table table-hover">
		<thead>
		<tr>
			<th scope="col">Job type</th>
			<th scope="col">Beskrivelse</th>
		</tr>
		</thead>
		<tbody>
		@foreach (var item in Model.KompetenceDto)
		{
			<tr class="table-primary">
				<th scope="row">@item.Job</th>
				<td>@item.Beskrivelse</td>
				<td>
					<div class="w-75 btn-group" role="group">
						<a asp-page="Edit" asp-route-id="@item.KompetenceId" class="btn btn-primary mx-2">
							<i class="bi bi-pencil-square">Edit</i>
						</a>
						<a asp-page="Delete" asp-route-id="@item.KompetenceId" class="btn btn-danger mx-2">
							<i class="bi bi-trash-fill">Delete</i>
						</a>
					</div>
				</td>
			</tr>
		}
		</tbody>
	</table>

</div>

@section Scripts {
	@{
		await Html.RenderPartialAsync("_ValidationScriptsPartial");
	}
}